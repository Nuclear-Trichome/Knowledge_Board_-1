Html here is for running on browser, and can be accessed on GitHub pages with links like "https://nuclear-trichome.github.io/Knowledge_Board_-1/VapCalc/vapcalcneo05u.html"

WebViewApp contains the latest version/environment for compiling on MacOS and running from terminal with command `clang -fobjc-arc -framework Cocoa -framework WebKit WebViewApp.m -o WebViewApp`

The WebViewApp latest version addresses an issue where records after 800 caused it to break, and adds new Session Log partial loading and graph features/changes.
